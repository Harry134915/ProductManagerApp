<UserControl x:Class="ProductManagerApp.Views.DeleteConfirmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Border Background="#FEF0F0" BorderBrush="#FBC4C4" BorderThickness="1"
            CornerRadius="6" Padding="12,10" Margin="0,0,0,8"
            Visibility="{Binding IsVisible, Converter={StaticResource BoolToVisibilityConverter}}">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="⚠️  确定要删除商品：" Foreground="#F56C6C" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Target.Name}" FontWeight="Bold"
                       Foreground="#F56C6C" Margin="4,0,12,0" VerticalAlignment="Center"/>
            <Button Content="确认删除" Width="80" Margin="0,0,8,0"
                    Style="{StaticResource DangerButton}"
                    Command="{Binding DataContext.ConfirmDeleteCommand,
                              RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Button Content="取消" Width="60"
                    Style="{StaticResource DefaultButton}"
                    Command="{Binding DataContext.CancelDeleteCommand,
                              RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>
    </Border>
</UserControl>
