<UserControl x:Class="ProductManagerApp.Views.ProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProductManagerApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!-- DataContext = ProductListViewModel，直接绑 Products / SelectedProduct -->
    <DataGrid ItemsSource="{Binding Products}"
              SelectedItem="{Binding SelectedProduct}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              SelectionMode="Single"
              Margin="0,10,0,0">
        <DataGrid.Columns>
            <DataGridTextColumn Header="ID"     Binding="{Binding Id}"          Width="50"/>
            <DataGridTextColumn Header="商品编码" Binding="{Binding Code}"        Width="100"/>
            <DataGridTextColumn Header="商品名称" Binding="{Binding Name}"        Width="*"/>
            <DataGridTextColumn Header="价格"   Binding="{Binding Price}"        Width="100"/>
            <DataGridTextColumn Header="库存"   Binding="{Binding Stock}"        Width="80"/>
            <DataGridTextColumn Header="描述"   Binding="{Binding Description}"  Width="2*"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
