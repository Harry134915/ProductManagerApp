<Window x:Class="ProductManagerApp.Views.ImportResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="导入预检结果" Width="680" Height="430"
        MinWidth="560" MinHeight="340"
        WindowStartupLocation="CenterOwner"
        Background="#F5F6FA"
        ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,12">
            <TextBlock Text="导入未执行" FontSize="18" FontWeight="SemiBold"
                       Foreground="#303133"/>
            <TextBlock Text="请修正以下问题后重新导入，数据库未写入任何商品。"
                       Margin="0,4,0,0" Foreground="#909399"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding}" IsReadOnly="True"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  SelectionMode="Single" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="行号" Binding="{Binding RowNumber}" Width="75"/>
                <DataGridTextColumn Header="字段" Binding="{Binding Field}" Width="130"/>
                <DataGridTextColumn Header="错误原因" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="保存错误报告" Style="{StaticResource DefaultButton}"
                    Width="120" Click="SaveReport_Click"/>
            <Button Content="关闭" Style="{StaticResource PrimaryButton}"
                    Width="84" Margin="10,0,0,0" IsDefault="True" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
