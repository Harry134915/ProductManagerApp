<Window x:Class="ProductManagerApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ProductManagerApp.Views.Converters"
        xmlns:views="clr-namespace:ProductManagerApp.Views"
        Title="商品管理系统" Height="600" Width="900"
        MinHeight="560" MinWidth="840"
        Background="#F5F6FA"
        Icon="/Assets/Idol.jpg"
        AutomationProperties.Name="商品管理系统主窗口"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <local:StringNotEmptyToVisibilityConverter x:Key="StringNotEmptyToVisibilityConverter"/>

        <Style x:Key="ToastBorderStyle" TargetType="Border">
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform Y="-6"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsVisible" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 To="1" Duration="0:0:0.16"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                                                 To="0" Duration="0:0:0.18">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 To="0" Duration="0:0:0.12"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                                                 To="-4" Duration="0:0:0.12"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="F5" Command="{Binding RefreshCommand}"/>
        <KeyBinding Key="Escape" Command="{Binding EscapeCommand}"/>
    </Window.InputBindings>

    <Grid Margin="14" KeyboardNavigation.TabNavigation="Continue">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="商品管理" FontSize="20" FontWeight="SemiBold"
                           Foreground="#303133"/>
                <TextBlock Text="维护商品编码、名称、价格、库存和描述"
                           Foreground="#909399" FontSize="12" Margin="0,2,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="导入" Width="72" Margin="0,0,8,0"
                        Style="{StaticResource DefaultButton}"
                        Command="{Binding ImportCommand}"/>
                <Button Content="导出" Width="72" Margin="0,0,8,0"
                        Style="{StaticResource DefaultButton}"
                        Command="{Binding ExportCommand}"/>
                <Button Content="下载模板" Width="92"
                        Style="{StaticResource DefaultButton}"
                        Command="{Binding DownloadTemplateCommand}"/>
            </StackPanel>
        </Grid>

        <views:ProductFormView    Grid.Row="1" DataContext="{Binding Form}" />
        <views:DeleteConfirmView  Grid.Row="2" DataContext="{Binding DeleteConfirm}" />
        <views:ProductListView    Grid.Row="3" DataContext="{Binding List}" />

        <!-- Canvas 的期望尺寸为零，Toast 不参与主内容的高度计算。 -->
        <Canvas Grid.Row="0" Grid.RowSpan="4"
                Panel.ZIndex="20"
                IsHitTestVisible="False">
            <StackPanel Canvas.Top="0" Canvas.Right="0" Width="380">
                <Border Background="#FEF0F0" BorderBrush="#FBC4C4" BorderThickness="1"
                        CornerRadius="4" Padding="12,8" Margin="0,0,0,8"
                        MinHeight="38"
                        Style="{StaticResource ToastBorderStyle}"
                        AutomationProperties.LiveSetting="Assertive"
                        Visibility="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="错误：" Foreground="#C53030"
                                   FontWeight="SemiBold" Margin="0,0,4,0"
                                   VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding ErrorMessage}"
                                   Foreground="#C53030" FontSize="13"
                                   TextWrapping="Wrap" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <Border Background="#F0F9EB" BorderBrush="#B3E19D" BorderThickness="1"
                        CornerRadius="4" Padding="12,8"
                        MinHeight="38"
                        Style="{StaticResource ToastBorderStyle}"
                        AutomationProperties.LiveSetting="Polite"
                        Visibility="{Binding StatusMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="状态：" Foreground="#2E7D32"
                                   FontWeight="SemiBold" Margin="0,0,4,0"
                                   VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding StatusMessage}"
                                   Foreground="#2E7D32" FontSize="13"
                                   TextWrapping="Wrap" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </StackPanel>
        </Canvas>
    </Grid>
</Window>
